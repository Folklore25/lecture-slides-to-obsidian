# Golden outputs

Golden outputs are committed only for synthetic or redistributable public fixtures. Each golden set should contain the final note, conversion report, and expected assets needed to verify the shared output contract.

Avoid full-file comparisons for values that are intentionally variable, such as timestamps or backend paths. Normalize those fields or assert them separately.
